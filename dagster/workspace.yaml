load_from:
  # Each entry here corresponds to a service in the docker-compose file that exposes jobs.
  - grpc_server:
      host: dagster-code-location
      port: 4000
      location_name: "dagster_university"

  - grpc_server:
      host: marketing-dbt
      port: 4000
      location_name: "marketing_dbt"
